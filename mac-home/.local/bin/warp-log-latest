#!/usr/bin/env bash
set -euo pipefail
# Open latest Warp markdown log from ~/@log/warp
DIR="$HOME/@log/warp"
latest=$(ls -t "$DIR"/*.md 2>/dev/null | head -1 || true)
if [ -n "${latest:-}" ]; then
  open "$latest"
else
  echo "未找到日志文件：$DIR/*.md" >&2
  exit 1
fi

